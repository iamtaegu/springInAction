server:
  port: 8989
  
#
# In chapter 15, the turbine.app-config property is shown to be configured with 
# multiple service names. Since this project does not (yet) contain the entire 
# Taco Cloud application in the form of multiple services, I've only configured 
# it here with the two services (which includes the client application) that are 
# part of this project. 
#
# Hystrix 스트림을 종합할 서비스들을 알 수 있게 Turbine 구성
turbine:
  app-config: ingredient-service, ingredient-client
#  app-config: ingredient-service, taco-service, order-service, user-service
  cluster-name-expression: "'default'"
